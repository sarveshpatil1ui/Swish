import React from "react";
import { Link } from "react-router-dom";
import { useSwish } from "../context/SwishContext";
export default function Moderation() {
  const { posts, users, removePost } = useSwish();
  return <div className="admin-shell"><aside className="admin-sidebar"><div className="brand">✦ SWISH ADMIN</div><nav><Link to="/admin">Dashboard</Link><Link to="/admin/colleges">Colleges</Link><Link className="active" to="/admin/moderation">Moderation</Link></nav></aside><main className="admin-main"><div className="admin-header"><div><span className="eyebrow">MODERATION</span><h1>Platform safety overview</h1></div></div><div className="ai-banner"><div><span>AI MODERATION — COMING SOON</span><h2>AI-assisted review architecture is ready.</h2><p>Future AI models can detect potentially sensitive or inappropriate content and recommend moderation actions.</p></div><span className="badge">FUTURE READY</span></div><div className="admin-stats"><div className="admin-stat"><span>Reported Posts</span><b>4</b><small>Pending review</small></div><div className="admin-stat"><span>AI Review Recommendations</span><b>7</b><small>Prototype only</small></div><div className="admin-stat"><span>Faculty Removed</span><b>{Math.max(1, users.length - 2)}</b><small>This month</small></div></div><div className="report-list">{posts.slice(0,5).map(p=>{const u=users.find(x=>x.id===p.userId);return <div className="report-card" key={p.id}><div className="avatar">{u?.avatar}</div><div><b>{u?.name}</b><span>{u?.college}</span><p>{p.caption}</p></div><div className="report-actions"><button className="table-btn" onClick={()=>removePost(p.id)}>Remove</button><button className="table-btn">Dismiss</button></div></div>})}</div></main></div>;
}